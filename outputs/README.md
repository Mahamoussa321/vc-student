# Outputs directory

This folder is reserved for generated model outputs, tables, and intermediate files.

The full `outputs/` directory is not tracked in GitHub because it contains large generated CSV files.

To reproduce manuscript figures and summaries, rerun the scripts described in the main `README.md`.
